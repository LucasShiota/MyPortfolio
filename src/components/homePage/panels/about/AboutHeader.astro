---
import AboutSectionRenderer from "./AboutSectionRenderer.astro";

const { data } = Astro.props;
const sections = data.sections || [];
---

<header class="sheet-header sheet-header-grid">
  {sections.map((section: any) => <AboutSectionRenderer section={section} />)}
</header>
