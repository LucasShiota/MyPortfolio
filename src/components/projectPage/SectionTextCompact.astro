---
import ProjectSection from './SectionBase.astro';

interface Props {
  title?: string;
  content: string;
  icon?: string;
  id?: string;
  order?: number;
  isSubsection?: boolean;
  parentOrder?: number;
  options?: {
    excludeFromOrder?: boolean;
    barColor?: string;
    compact?: boolean;
  };
}

const { 
  title, 
  content, 
  icon, 
  id, 
  order, 
  isSubsection = false,
  parentOrder,
  options
} = Astro.props;
---

<ProjectSection 
  title={title} 
  icon={icon} 
  id={id} 
  order={order} 
  isSubsection={isSubsection}
  parentOrder={parentOrder}
  options={options}
  class="text-compact-section"
>
  <div class="text-content">
    {content.split('\n').map(line => (
      <p>{line}</p>
    ))}
  </div>
</ProjectSection>

<style>
  @reference "../../styles/global.css";

  /* The :global wrapper ensures these styles punch through component boundaries */
  :global(.text-compact-section) :global(.panel-card) {
    display: flex !important;
    flex-direction: row !important;
    align-items: flex-start !important;
    justify-content: flex-start !important;
    gap: 3rem !important;
    padding-top: var(--section-pad-y) !important;
    padding-bottom: var(--section-pad-bottom, var(--section-pad-y)) !important;
  }

  :global(.text-compact-section) :global(.section-title) {
    margin-bottom: 0 !important;
    width: var(--section-label-width) !important;
    flex-shrink: 0;
    align-self: flex-start;
    padding-top: 0.25rem;
  }

  :global(.text-compact-section) :global(.content-wrapper) {
    width: auto !important;
    flex-grow: 1;
    display: flex;
    align-items: center;
  }

  .text-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    line-height: 1.6;
    color: var(--text-color);
  }

  .text-content p {
    margin: 0;
    opacity: 0.9;
  }

  /* Desktop/Tablet */
  @media (max-width: 1024px) {
    :global(.text-compact-section) :global(.panel-card) {
      gap: 1.5rem !important;
    }
  }

  /* Mobile - vertical stack */
  @media (max-width: 768px) {
    :global(.text-compact-section) :global(.panel-card) {
      flex-direction: column !important;
      align-items: flex-start !important;
      gap: 0.75rem !important;
      padding-top: var(--section-pad-y) !important;
      padding-bottom: var(--section-pad-bottom, var(--section-pad-y)) !important;
    }

    :global(.text-compact-section) :global(.section-title) {
      width: 100% !important;
    }



    :global(.text-compact-section) :global(.section-title) h2 {
      @apply txt-body;
    }

    :global(.text-compact-section) :global(.content-wrapper) {
      width: 100% !important;
    }
  }
</style>
