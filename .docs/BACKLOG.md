# Portfolio Backlog & Recommendations

This file tracks planned improvements, technical debt, and feature ideas for the portfolio.

## 🛠️ Engineering & Quality Assurance

- [ ] **CI/CD: Automated Testing**
  - Add a GitHub Action (`.github/workflows/tests.yml`) to run `npm test` (Vitest) on every push and pull request.
  - Ensures no broken links or invalid constants make it to production.

- [ ] **Asset Integrity Checks**
  - Expand the `tests/` folder to include a script that verifies all image paths defined in `src/config/links.ts` or content collections actually exist in the `public/` or `src/assets/` folders.

- [ ] **Accessibility (A11y) Audit**
  - Use Lighthouse or Axe-core to ensure the portfolio is navigable by screen readers and keyboard-only users (crucial for a professional site).

- [ ] **Performance: Image Optimization**
  - Audit the use of the Astro `<Image />` component to ensure all high-resolution project screenshots are being lazy-loaded and converted to modern formats (WebP/AVIF).

## ✨ Features & Enhancements

- [ ] **Interactive Backgrounds Strategy**
  - You are using `vanta` and `three`. Consider a "Reduced Motion" setting that swaps these for static high-quality gradients for users with vestibular sensitivities.

- [ ] **Contact Form / "Let's Talk" Integration**
  - If not already implemented, connect the "Let's Talk" component to a service like Formspree, Netlify Forms, or a Cloudflare Worker to handle submissions.

- [ ] **Dark/Light Mode Sync**
  - Ensure the theme is consistent across all pages and persists in `localStorage` to prevent "flashes" of the wrong theme on page load.

---
*Generated by Antigravity AI Assistant*
